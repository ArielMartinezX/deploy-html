# Deploy y otras cosas

1. En el package json tengo todos los datos de los requisitos, si borro por accidente node modules, con npm install lo resuelve.

# .gitignore
Archivo queusa github o repos basados en git que delimita que archivos se suben al repo

1. agregar archivo .gitignore

# Paginas de hosting
vercel
netlyfy -> https://netlify.com
render